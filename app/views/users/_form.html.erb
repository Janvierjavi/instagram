<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card cont" style="width:400px">
  <div class="card-header"> Sign up to see photos and videos from your friends.</div>
  <div class="card-body">
  <div class="field">
  <%= form.label :Email %>
  <%= form.text_field :Email %>
</div>
  <div class="field">
  <%= form.label :First_Name %>
  <%= form.text_field :First_Name %>
</div>

  <div class="field">
    <%= form.label :Second_Name %>
    <%= form.text_field :Second_Name %>
  </div>

<div class="field">
  <%= form.label :password %>
  <%= form.password_field :password %>
</div>

<div class="field">
  <%= form.label :password_confirmation %>
  <%= form.password_field :password_confirmation %>
</div>

</div> 
  <div class="card-footer"><div class="actions">
  <%= form.submit %>
  </div></div>
</div>
<% end %>
